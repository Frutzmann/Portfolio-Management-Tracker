pipeline {
    agent any

    stages {
        stage('Build maven') {
            steps {
                sh "mvn clean install"
            }
        }
        stage('Build docker') {
            steps {
                sh "docker build -t pftdev ."
            }
        }
    }
}