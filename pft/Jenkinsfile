pipeline {
    agent any

    stages {
        stage('Build maven') {
            steps {
                mvn clean install
            }
        }
        stage('Build docker') {
            sh "docker build -t pftdev ."
        }
    }
}